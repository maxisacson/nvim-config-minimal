# nvim-config-minimal

A minimal neovim configuration. Minimal in terms of external dependencies, not
necessarily features. The only external dependency is `gruvbox.nvim` for the
`gruvbox` colorscheme. For a config with no external dependencies check out the
`tiny` branch which use the built in `retrobox` colorscheme instead of
`gruvbox`.
